import { AboutScrollDirective } from './about-scroll.directive';

describe('AboutScrollDirective', () => {
  it('should create an instance', () => {
    const directive = new AboutScrollDirective();
    expect(directive).toBeTruthy();
  });
});
