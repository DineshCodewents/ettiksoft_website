import { ProductPageDirective } from './product-page.directive';

describe('ProductPageDirective', () => {
  it('should create an instance', () => {
    const directive = new ProductPageDirective();
    expect(directive).toBeTruthy();
  });
});
